apiVersion: v1
kind: Service
metadata:
  name: event-backend
  labels:
    app: event-backend
spec:
  type: NodePort
  selector:
    app: event-backend
  ports:
    - port: 8082
      targetPort: 8082
      nodePort: 30082
